# Used by Xcode "Bundle React Native code and images" (and Expo configure scripts).
# Xcode Cloud strips shell env during xcodebuild; ci_post_clone.sh writes .xcode.env.local
# with an absolute NODE_BINARY path after npm is installed.
export NODE_BINARY=$(command -v node)
