# Sourced by the "Bundle React Native code and images" phase after .xcode.env.local.
# Xcode Cloud archive runs with -hideShellScriptEnvironment (empty PATH).
export PATH="/opt/homebrew/bin:/usr/local/bin:${PATH}"
