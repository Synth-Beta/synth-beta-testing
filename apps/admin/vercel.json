{
  "ignoreCommand": "exit 1",
  "installCommand": "npm install && npm install --prefix ../../ai-scene-guides --omit=dev",
  "functions": {
    "api/ops-alert.js": { "maxDuration": 15 }
  },
  "rewrites": [
    { "source": "/auth/spotify/callback", "destination": "/" },
    { "source": "/((?!api/).*)", "destination": "/" }
  ]
}

